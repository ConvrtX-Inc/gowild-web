import {Box, styled} from "@mui/material";

export const OrangeBorder = styled(Box)(() => ({
    borderBottom: '7px solid #e4572e',
    width: 300,
    position: 'absolute',
    bottom: '-4px',
    left: '42px',
}));
